name: LADLA-MALIK-MD
services:
  - name: LADLA-MALIK-MD-service
    type: web
    ports:
      - 5000
    routes:
      - path: "/"
    build:
      type: docker
      dockerfile: ./Dockerfile
    regions:
      - fra
      - sin
      
